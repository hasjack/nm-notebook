# Gaussian doors

Finite census: for primes `3 < p ≤ 100000`, whether `m0(p)` as two squares
converts to `p = x^2 + 3 b^2` by a fixed-x Pell correction.

```
python3 experiment.py > results.json
```

Lab page: `/gaussian-doors`.
