name: Hire.Cone

on:
  pull_request:
  push:
    branches: [note/lemma8-proof-map]
  workflow_dispatch:

jobs:
  build:
    name: lake build Hire.Cone
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: leanprover/lean-action@v1
        with:
          lake-package-directory: lean
          auto-config: false
          build: "true"
          build-args: Hire.Cone
          use-mathlib-cache: "true"
